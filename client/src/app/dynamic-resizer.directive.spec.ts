import { DynamicResizerDirective } from './dynamic-resizer.directive';

describe('DynamicResizerDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicResizerDirective();
    expect(directive).toBeTruthy();
  });
});
